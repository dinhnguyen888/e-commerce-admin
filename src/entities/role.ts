export interface Role {
    id: number;
    roleName: string;
}

export interface RolePostDto {
    id: number;
    roleName: string;
}

export interface RoleUpdateDto {
    id: number;
    roleName: string;
}
